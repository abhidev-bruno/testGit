info:
  name: testRequest13213
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/posts/1245234"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "title": "title",
        "body": "body",
        "userId": 1
      }
  auth: inherit

runtime:
  variables:
    - name: pre
      value: |-
        change
        value
        change
        value
        change
        value

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
